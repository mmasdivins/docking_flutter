import 'package:docking/src/layout/docking_layout.dart';

/// Event that will be triggered when a [DockingItem] is focused.
typedef OnItemFocusChanged = void Function(DockingItem item, bool hasFocus);
